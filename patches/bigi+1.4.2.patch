diff --git a/node_modules/bigi/lib/convert.js b/node_modules/bigi/lib/convert.js
index 5c9551d..604e3e2 100644
--- a/node_modules/bigi/lib/convert.js
+++ b/node_modules/bigi/lib/convert.js
@@ -1,4 +1,5 @@
 // FIXME: Kind of a weird way to throw exceptions, consider removing
+var Buffer = require('buffer').Buffer
 var assert = require('assert')
 var BigInteger = require('./bigi')
 
@@ -83,7 +84,7 @@ BigInteger.prototype.toBuffer = function(size) {
   var padding = size - byteArray.length
   while (zeros.length < padding) zeros.push(0)
 
-  return new Buffer(zeros.concat(byteArray))
+  return Buffer.from([...zeros, ...byteArray])
 }
 
 BigInteger.prototype.toHex = function(size) {
